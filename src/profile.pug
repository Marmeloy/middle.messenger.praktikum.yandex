doctype html
html(lang="ru")
    head
        link(rel="stylesheet" href="./styles.scss")
    body
        div#page
            script(type="module").
              // После введения роутинга этот скрипт будет не нужен
              import {ProfileController} from "./controllers/profile/ProfileController.ts";
              const controller = new ProfileController();
              controller.index();
