doctype html
html(lang="ru")
    head
        link(rel="stylesheet" href="./styles.scss")
    body
        div#page
        script(type="module").
          // После введения роутинга этот скрипт будет не нужен
          import {Error404Controller} from "./controllers/error/Error404Controller";
          const controller = new Error404Controller();
          controller.index();
